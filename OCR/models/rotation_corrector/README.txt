Pretrained MC_OCR MobileNetV3 rotation weights (pipeline bước 2).

File: mobilenetv3-Epoch-487-Loss-0.03-Acc-0.99.pth

Copied from MC_OCR — ai-service loads from here (no MC_OCR repo at runtime).
Retrain NOT required for new bills.
